# Beholder Configuration
# Copy this file to .env and adjust values for your environment

# Network interface to capture packets on
BEHOLDER_INTERFACE=eth0

# Web server port (include the colon)
BEHOLDER_PORT=:8080

# Paths to MaxMind GeoIP databases
BEHOLDER_DB_CITY=GeoLite2-City.mmdb
BEHOLDER_DB_ASN=GeoLite2-ASN.mmdb

# Project directory for static files (leave empty to use current working directory)
BEHOLDER_STATIC_DIR=

# Allowed WebSocket origins (comma-separated, e.g., "http://localhost:8080,https://example.com")
# Leave empty for same-origin only (default, most secure)
BEHOLDER_ALLOWED_ORIGINS=
